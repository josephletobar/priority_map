# Drone Heatmap

## Quick run

```bash
python main.py --dataset-root "a folder of images of video frames"
```

Use `--show` to display frames with OpenCV and `--record` to save `video.mp4` in the run output folder.

Panoramic images output into the examples folder, respectively for heatmap and standard images.
